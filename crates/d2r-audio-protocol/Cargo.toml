[package]
name = "d2r-audio-protocol"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Versioned, application-neutral D2R audio telemetry protocol"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
hound = "3.5.1"
